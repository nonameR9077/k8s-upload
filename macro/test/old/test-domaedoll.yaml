apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: test-macro-domaedoll
  name: test-macro-domaedoll
  namespace: macro
spec:
  containers:
  - image: selenium/standalone-chrome
    name: test-macro-domaedoll
    command: ["bin/sh"]
    args:
      - "-c"
      - |
        set -e 
        apt-get update && apt-get install -y python3 python3-pip 
        pip3 install selenium webdriver-manager --break-system-package 
        python3 /app/macro-domaedoll.py 
    securityContext:
      runAsUser: 0
    volumeMounts:
      - name: macro-script-volume
        mountPath: /app/macro-domaedoll.py
        subPath: macro-domaedoll.py
    resources: {}
  volumes:
    - name: macro-script-volume
      configMap:
        name: cm-domaedoll
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}
