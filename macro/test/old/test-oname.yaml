apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: test-macro-oname
  name: test-macro-oname
  namespace: macro
spec:
  containers:
  - image: selenium/standalone-chrome
    name: test-macro-oname
    command: ["bin/sh"]
    args:
      - "-c"
      - |
        set -e 
        apt-get update && apt-get install -y python3 python3-pip 
        pip3 install selenium webdriver-manager --break-system-package 
        python3 /app/macro-oname.py 
    securityContext:
      runAsUser: 0
    volumeMounts:
      - name: macro-script-volume
        mountPath: /app/macro-oname.py
        subPath: macro-oname.py
    resources: {}
  volumes:
    - name: macro-script-volume
      configMap:
        name: cm-oname
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}
